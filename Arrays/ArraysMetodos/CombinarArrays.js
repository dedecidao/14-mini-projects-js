//Combinar os arrays Metodo .join

const numeros = [1,2,3,4,5];

const combinado = numeros.join('-');

console.log(combinado);  // Output = 1-2-3-4-5

//Metodo para juntar tudo em um Array usando um divisor de elementos.

const frase = "ola essa frase ira virar um array";

arrayDeFrase = frase.split(" ");

console.log(arrayDeFrase);